<UserControl x:Class="WindowsTelemetryInspector.Views.DnsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="300" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="DNS observations" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="Locally observed DNS client events and best-effort IP/name correlations." Style="{StaticResource MutedTextStyle}" />
            </StackPanel>
            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" ToolTip="Search query name, address, or PID" AutomationProperties.Name="Search DNS observations" />
        </Grid>
        <Border Grid.Row="1" Background="#2B2110" BorderBrush="#75551F" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="DNS data may be absent when names are cached, encrypted, or emitted before capture. A correlated name is not equivalent to TLS SNI or application-layer proof." Foreground="#F0C875" TextWrapping="Wrap" />
        </Border>
        <DataGrid Grid.Row="2" ItemsSource="{Binding ObservationsView}" AutoGenerateColumns="False" AutomationProperties.Name="Observed DNS activity">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Time" Width="150"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding TimestampUtc, Converter={StaticResource DateTimeConverter}, ConverterParameter=yyyy-MM-dd HH:mm:ss.fff}" FontFamily="Consolas" FontSize="11" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="80" />
                <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="100" />
                <DataGridTextColumn Header="Query name" Binding="{Binding QueryName}" Width="*" MinWidth="260" />
                <DataGridTextColumn Header="Address" Binding="{Binding Address, TargetNullValue=Not available}" Width="180" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="180" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
