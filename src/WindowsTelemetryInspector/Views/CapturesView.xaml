<UserControl x:Class="WindowsTelemetryInspector.Views.CapturesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Capture files" Style="{StaticResource PageTitleStyle}" />
                <TextBlock Text="Record, reopen, and inspect JSONL captures without changing the existing CLI-compatible format." Style="{StaticResource MutedTextStyle}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Content="Open Capture…" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding OpenCaptureCommand}" Margin="0,0,8,0" />
                <Button Content="Open Folder" Command="{Binding OpenFolderCommand}" Margin="0,0,8,0" />
                <Button Content="Refresh" Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="120" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <TextBlock Text="Current file" Style="{StaticResource DetailLabelStyle}" />
                <TextBlock Grid.Column="1" Text="{Binding Workspace.CurrentCapturePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding Workspace.CurrentCapturePath}" />
                <Border Grid.Column="2" Background="#183623" BorderBrush="#2E7948" BorderThickness="1" CornerRadius="4" Padding="8,3"
                        Visibility="{Binding Workspace.IsRecording, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="RECORDING" Foreground="{StaticResource SuccessBrush}" FontWeight="Bold" FontSize="11" />
                </Border>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Files}" SelectedItem="{Binding SelectedCapture}" AutoGenerateColumns="False" AutomationProperties.Name="Capture files in the configured folder">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenSelectedCaptureCommand}" />
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding Name}" Width="*" MinWidth="300" />
                <DataGridTextColumn Header="Path" Binding="{Binding FullPath}" Width="2*" MinWidth="350" />
                <DataGridTemplateColumn Header="Size" Width="100"><DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding Size, Converter={StaticResource BytesConverter}}" /></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                <DataGridTextColumn Header="Modified" Binding="{Binding ModifiedLocal, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="165" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
