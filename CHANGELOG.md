# Changelog

## Unreleased

- Added the Windows Telemetry Inspector WPF desktop application.
- Extracted capture, correlation, persistence, and analysis into a shared core library.
- Preserved the `live`, `record`, and `summary` CLI workflows.
- Added process, service, DNS, timeline, capture, summary, settings, and about views.
- Added bounded live-view buffers, batched UI updates, and virtualized data grids.
- Added backward-compatible JSONL reading and structured extensions for richer metadata.
- Added a self-contained Windows x64 single-file publish profile and application icon.
- Expanded tests for classification, parsing, filtering, aggregation, and JSONL compatibility.
